<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Selected (active) -->
    <item android:state_selected="true"
        android:drawable="@drawable/ic_recently_selected" />

    <!-- Default (inactive) -->
    <item android:drawable="@drawable/ic_recently_unselected" />

</selector>